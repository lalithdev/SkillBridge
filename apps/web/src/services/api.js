/**
 * @deprecated Use `@/api` directly. Kept for backward compatibility.
 */
export {
  studentApi,
  opportunityApi,
  companyApi,
  collegeApi,
  platformApi,
  skillsApi,
} from '@/api';

export { apiClient } from '@/api/client';
